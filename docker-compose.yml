---
version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: ./Dockerfile
      target: dev
    ports:
      - "8000:8000"
    volumes:
      - './:/app'
    networks:
     - app

networks:
  app:
    driver: bridge
